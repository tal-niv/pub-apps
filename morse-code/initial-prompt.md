I want to build an android app that lets users use morse code.
The app will resemble google translate, and will have language options English / Morse
for morse, we need an . _ buttons on the left of the textbox. typing anything will translate in the other textbox to the output language.
Below the output box we'll have a "Send" button, which will use the device flashlight to send the morse message (if output is in another language, it will still be translated to morse, and sent to flashlight).
Use these instructions, as well as the morse converter table.

